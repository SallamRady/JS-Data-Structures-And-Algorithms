/**
 * HackerRank - crosswordPuzzle
 * this problem need backtrack algorithm ,
 * we will solve it after learn this algorithm in Sha Allah.
 */

function crosswordPuzzle(crossword, words , places) {
    if(words.length == 0)
        return;
    

}